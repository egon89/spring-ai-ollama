{
  "mcpServers": {
    "filesystem": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-v",
        "/tmp/to-sell:/data",
        "mcp/filesystem",
        "/data"
      ]
    }
  }
}
